# OS / editor junk
.DS_Store
*.swp
Thumbs.db

# Never commit secrets — service-role keys, Anthropic keys, .env files
.env
.env.*
*secret*
*service_role*

# Local working folder. Holds BRL_PROJECT_HANDOFF.md with plaintext credentials,
# plus scratch copies of index.html. This repo is PUBLIC and Netlify deploys
# from it — anything in here would be served at a public URL.
BRL OPS/
